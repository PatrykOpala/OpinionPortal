import { TeleportToDirective } from './teleport-to.directive';

describe('TeleportToDirective', () => {
  it('should create an instance', () => {
    const directive = new TeleportToDirective();
    expect(directive).toBeTruthy();
  });
});
