import { TeleportOutletDirective } from './teleport-outlet.directive';

describe('TeleportOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new TeleportOutletDirective();
    expect(directive).toBeTruthy();
  });
});
